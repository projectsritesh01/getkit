import api from './api';

const productService = {
  // Example placeholder
  getAllProducts: () => {
    // return api.get('/products');
  },

  getProductById: (id) => {
    // return api.get(`/products/${id}`);
  },
};

export default productService;
